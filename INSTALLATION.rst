============
Installation
============

At the command line::

    $ pip install ynabintegrationslib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ynabintegrationslib
    $ pip install ynabintegrationslib

Or, if you are using pipenv::

    $ pipenv install ynabintegrationslib
